server {
    # Listen on unprivileged HTTP port
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Minimal overhead
    access_log off;
    server_tokens off;

    location / {
        try_files $uri $uri/ =404;
    }
}
